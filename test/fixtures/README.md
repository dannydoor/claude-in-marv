# Test fixtures

`lineage-canary/` contains the small immutable examples used to protect ancestry resolution, column numbering and PDB identifier parsing.
Large server captures and exploratory regression corpora are not part of the public repository.
